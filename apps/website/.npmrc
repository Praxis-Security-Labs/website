# Force npm to include optional dependencies
include=optional
optional=true

# Platform targeting for Linux builds
target_platform=linux
target_arch=x64
target_libc=glibc

# Improve CI reliability
cache-min=3600
progress=false
fund=false
audit=false